# include <stdio.h>
int Pali(int start, int length){
	for(int i = 0; i < length; i++){
		if(i != )
	}
}
int input[100];
int main(){
	int i = 0, start;
	while(scanf("%d", &input[i]) != EOF){
		i++;
	}
	int total_length = i;
	int max_length = 0;
	//printf("length = %d\n", length);
	for(int start = 0; start < total_length; start++){
		for(int length = 1; start + length < total_length; length++){
			if(Pari()){
				
			}
		}
	}
	
}
