#include <stdio.h>
#include <string.h>
int main(){
	int str, max = 0;
	len = 0;
	int last = -1;
	while(scanf("%s", str) != EOF){
		int w = strlen(str);
		if(){

		}
		else{

		}
		last = str[w - 1];
		if(len > max){
			max = len;
		}
	}

	
}